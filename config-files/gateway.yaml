server:
  port: 8888

eureka:
client:
  serviceUrl:
    defaultZone: http://localhost:8761/eureka/
logging:
    level: debug

eureka:
profiles: docker
client:
  serviceUrl:
    defaultZone: http://localhost:8761/eureka/
logging:
    level: debug

